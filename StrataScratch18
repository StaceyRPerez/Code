#For each video game player, find the latest date when they logged in.







SELECT
    player_id,
    MAX(login_date)
FROM
    players_logins
GROUP BY
    player_id;




In the columns for the ask you can use the MAX function to filter for the newest date or in this case, latest date
